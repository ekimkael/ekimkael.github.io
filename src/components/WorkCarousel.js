import React from "react";
import styled from "styled-components";

const Carousel = styled.div`
  width: 100vw;
  height: 100%;
  position: relative;
  background: hsla(204, 3%, 60%, 0.75);
`;

export default () => (
  <Carousel />
)
